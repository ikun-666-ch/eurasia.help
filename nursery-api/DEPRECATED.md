# 已弃用

本目录为早期 **Java Spring Boot** 后端，已不再用于生产与日常开发。

已迁至桌面独立目录：**`~/Desktop/eurasia.java/nursery-api/`**

请使用 **`~/Desktop/eurasia/nursery-php`**（PHP 8.1 + SQLite/MySQL），API 路径与 JSON 格式兼容。

本地联调见 **`~/Desktop/eurasia/RUNNING.md`**。
