package com.eurasia.nursery.domain.repository;

import com.eurasia.nursery.domain.entity.PanelConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PanelConfigRepository extends JpaRepository<PanelConfig, String> {
}
